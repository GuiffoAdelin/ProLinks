enum ConnectionStatus {
  PENDING
  ACCEPTED
  REJECTED
}

model Connection {
  id          Int              @id @default(autoincrement())
  senderId    Int
  receiverId  Int
  status      ConnectionStatus @default(PENDING)
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  sender      User @relation("SentConnections", fields: [senderId], references: [id])
  receiver    User @relation("ReceivedConnections", fields: [receiverId], references: [id])

  @@unique([senderId, receiverId]) // Évite les doublons d'invitation
}

model Job {
  id          Int      @id @default(autoincrement())
  title       String
  company     String
  description String
  location    String
  salary      String?
  creatorId   Int
  createdAt   DateTime @default(now())

  creator     User     @relation(fields: [creatorId], references: [id])
}

// N'oublie pas d'ajouter les relations dans ton modèle User :
// model User {
//   ...
//   sentConnections     Connection[] @relation("SentConnections")
//   receivedConnections Connection[] @relation("ReceivedConnections")
//   jobs                Job[]
// }